import torch
import re
import math

# a = torch.tensor([3,4,5])
# b = a[0]
# print(b.shape)
# print(b.data)

# search = re.compile(r'1+', flags=re.ASCII).search
# text = r"  12323"
# print(bool(search(text)))

number = math.inf + math.inf
print(number == math.inf)